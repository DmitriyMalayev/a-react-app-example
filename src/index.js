import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';  //.js is not included but it's implied 

ReactDOM.render(<App />, document.getElementById('root'));

/*
The index.js file doesn't have an export. It's on top of this chain. 

*/